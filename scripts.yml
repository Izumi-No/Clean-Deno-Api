scripts:
  "dev": denon run --allow-net --allow-read --allow-write --allow-env --import-map importmap.json src/server.ts
  "start": deno run --allow-net --allow-read --allow-write --allow-env --import-map importmap.json src/server.ts
  "sync:db": deno run --allow-env --allow-write --allow-read --import-map importmap.json Scripts/DenoDB/Sync.ts
  "test:db": deno run --allow-env --allow-write --allow-read --import-map importmap.json Scripts/DenoDB/test.ts
